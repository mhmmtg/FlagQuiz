<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlagsQuiz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/signika</item>
        <!--<item name="android:windowFullscreen">true</item>-->
    </style>

    <style name="Theme.FlagsQuiz" parent="Base.Theme.FlagsQuiz" />

    <style name="CustomRadio" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="android:button">@android:color/transparent</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginBottom">8dp</item>
        <!--<item name="android:background">@drawable/bg_radio</item>-->
        <item name="android:background">@drawable/bg_option</item>
        <!--<item name="android:textColor">@drawable/color_radio_text</item>-->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="rectangle">
        <item name="cornerSize">10dp</item>
    </style>
</resources>